# UC3: Display "Hello" with Command-Line Argument or Default Message

## Description:

```
The app accepts a user's name as a command-line argument and displays a personalized greeting.
If no name is provided, it defaults to "World".
This use case combines the basic functionality from UC1 with the personalization from UC2, adding robustness through default handling.
```
