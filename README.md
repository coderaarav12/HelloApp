# UC4: Display "Hello" with Multiple Command-Line Arguments or Default Message

## Description:

```
The app accepts multiple names as command-line arguments and displays a personalized greeting for all names.
If no names are provided, it defaults to "World".
This use case extends the functionality of UC3 by allowing for multiple inputs and demonstrating how to handle arrays of arguments.
```
